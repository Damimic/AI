# backend

Not started yet.

Will provide: an authenticated HTTPS ingestion endpoint for agent-reported
findings (mTLS or API-key auth), and a Postgres schema for storing findings.
See the top-level [README](../README.md) and project brief for scope.
